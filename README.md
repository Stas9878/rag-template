# RAG Template

Универсальный шаблон для быстрого развертывания RAG-системы.

## 🧩 Технологии
- **FastAPI** — асинхронный REST API
- **Qdrant** — векторная база данных
- **Ollama** — локальные LLM (`qwen3:latest`) и эмбеддинги (`nomic-embed-text`)
- **LangChain** — интеграция компонентов

## 🚀 Возможности
- Загрузка PDF через `/api/upload`
- Семантический поиск через `/api/query`
- Версионирование документов (`is_current` флаг)
- Фильтрация по актуальным версиям
- Детализированные метрики времени

## 📥 Использование
1. Загрузите документ:
   ```bash
   curl -X POST http://127.0.0.1:8000/api/upload -F "file=@document.pdf"
   ```
2. Выполните поиск:
   ```bash
   curl "http://127.0.0.1:8000/api/query?query=Ваш+запрос"
   ```

## 📁 Структура
```
src/
├── api/          # Эндпоинты
├── core/         # Ядро (Qdrant, Ollama)
├── utils/        # Утилиты
└── main.py       # Точка входа
```

## ▶️ Запуск
```bash
uv install
uvicorn src.main:app --host 127.0.0.1 --port 8000
```

> Требуется запущенные Qdrant и Ollama.